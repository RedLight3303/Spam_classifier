# Spam_classifier
Spam Email Classifier is a Python machine learning project that implements a Multinomial Naive Bayes classifier from scratch to detect spam emails. It uses custom text preprocessing, bag-of-words features, Laplace smoothing, and probability-based classification without external ML libraries.
